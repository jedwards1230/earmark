{{/*
Service — routes traffic to the mcp-server Deployment only.
The ingest worker has no HTTP port and is not selected.
*/}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "lilbro-whisper.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "lilbro-whisper.labels" . | nindent 4 }}
    app.kubernetes.io/component: mcp-server
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lilbro-whisper.mcp.selectorLabels" . | nindent 4 }}

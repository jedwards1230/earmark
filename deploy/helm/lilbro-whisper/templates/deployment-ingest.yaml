# Ingest Deployment — file-watcher + embedding worker.
#
# Runs: lil-whisper monitor
#   - Walks BOOKS_DIR and enqueues new audio files as transcription_jobs.
#   - Polls for completed transcripts, chunks them, calls Ollama, stores embeddings.
#
# No HTTP port; no Service or Ingress. Liveness = exec pgrep.
# Strategy: Recreate (single writer; only 1 replica meaningful).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "lilbro-whisper.ingest.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "lilbro-whisper.labels" . | nindent 4 }}
    app.kubernetes.io/component: ingest
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "lilbro-whisper.ingest.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "lilbro-whisper.labels" . | nindent 8 }}
        app.kubernetes.io/component: ingest
      annotations:
        {{- include "lilbro-whisper.configChecksum" . | nindent 8 }}
    spec:
      securityContext:
        {{- include "lilbro-whisper.podSecurityContext" . | nindent 8 }}

      containers:
        - name: lilbro-whisper-ingest
          image: "{{ .Values.image.repository }}:{{ include "lilbro-whisper.imageTag" . }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}

          # args overrides the Dockerfile CMD — always explicit, never rely on CMD.
          args: ["monitor"]

          securityContext:
            {{- include "lilbro-whisper.containerSecurityContext" . | nindent 12 }}

          env:
            {{- include "lilbro-whisper.commonEnv" . | nindent 12 }}

          # Liveness: confirm the process is still running.
          # No HTTP port on this pod, so use an exec probe.
          livenessProbe:
            exec:
              command:
                - pgrep
                - "-f"
                - lil-whisper
            initialDelaySeconds: 10
            periodSeconds: 30
            failureThreshold: 3

          resources:
            {{- toYaml .Values.resources | nindent 12 }}

          volumeMounts:
            {{- include "lilbro-whisper.volumeMounts" . | nindent 12 }}

      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      volumes:
        {{- include "lilbro-whisper.volumes" . | nindent 8 }}
